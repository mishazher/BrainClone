fastapi[all]>=0.115.0,<0.116.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
neo4j>=5.25.0
asyncpg>=0.30.0
pgvector>=0.3.6
pydantic-settings>=2.6.0
python-jose[cryptography]>=3.3.0
httpx>=0.27.0
redis>=5.2.0
numpy>=2.0.0
scikit-learn>=1.5.0
structlog>=24.4.0
python-dotenv>=1.0.1
celery>=5.4.0
google-generativeai>=0.3.2
# NOTE: the r2r server is intentionally NOT here — r2r[core] pins asyncpg<0.30,
# numpy<1.29 and uvicorn<0.28, which conflict with the backend. It gets its own
# venv in the Docker image (see backend/Dockerfile); the backend talks to it
# over HTTP only.
